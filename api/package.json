{
  "name": "api",
  "version": "1.0.0",
  "description": "",
  "main": "server.ts",
  "scripts": {
    "dev": "export NODE_ENV=development && export PORT=3333 && ts-node-dev server.ts",
    "build": " tsc -p .",
    "start": "export NODE_ENV=production && npm run build && ts-node dist/server.js",
    "lint": "eslint ./src/**/*.ts --fix && eslint ../client/app/**/*.ts --fix"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^4.17.14",
    "@types/node": "^18.7.18",
    "express": "^4.18.1",
    "ts-node": "^10.9.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.8.3"
  }
}
